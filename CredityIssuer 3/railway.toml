[build]
builder = "DOCKERFILE"
dockerfilePath = "CredityIssuer 3/Dockerfile"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
